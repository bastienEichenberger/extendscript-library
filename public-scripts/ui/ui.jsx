/** * Script to tests UI methods *///@target "indesign"//@include "../../lib/indesign/indesign-lib.jsx"try {    IN.Config.init();    /**     * The script folder     * @type {Folder}     */    var SCRIPT_FOLDER = new File($.fileName).parent;    /**     * The log folder     * @type {Folder}     */    var LOG_FOLDER = new Folder(SCRIPT_FOLDER + '/log');    H.Log.init(LOG_FOLDER, 0);    /***********************************************************************     * create user interface     ***********************************************************************/    var ui = new UI('title');    var panel = ui.panel('my panel');    var panel2 = ui.panel('my second panel');    var group1 = panel.group('group1', {orientation: 'row'});    var group2 = panel.group('group2', {orientation: 'row'});    var group3 = panel2.group('group3', {orientation: 'column'});    // functions    var onclick = function () {        alert('hello world');    }    var onchange = function () {        alert('helle change');    }    group1.control('button', 'run', {name: 'run'}, {        name: 'click',        fn: onclick    }).control('button', 'cancel', {name: 'cancel'});    group2.control('statictext', 'label').control('edittext', 'content', undefined,        [            {                name: 'change',                fn: onchange            }        ]    );    group3.control('radiobutton', 'yes').control('radiobutton', 'no');    // display the UI    ui.show();}catch (ex) {    H.Log.errorAlert(        'file: ' + ex.fileName +        '\n name: ' + ex.name +        '\n message: ' + ex.message +        '\n line: ' + ex.line    );}finally {    IN.Application.restore();}