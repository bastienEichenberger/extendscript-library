/** * Function to check if a link contains an bitmap image * @name Link.is_image_bitmap * @function * @return {Boolean} true if the link is a bitmap image */Link.prototype.is_image_bitmap = function(){    var is_bitmap = false;    var image = this.parent;    var black_and_white_key = app.translateKeyString("$ID/#Links_Black and White");    if (image.constructor.name == "Image") {        if(image.space == black_and_white_key){            is_bitmap = true;        }    }    return is_bitmap}/** * Function who check if a link is proportional or not * @name Link.is_proportional * @function * @return {Boolean} true if the link is proportional **/Link.prototype.is_proportional = function(){    var myImage = this.parent;    var myHorScale = Math.abs(myImage.absoluteHorizontalScale);    var myVerScale = Math.abs(myImage.absoluteVerticalScale);    if(Math.round(myHorScale) === Math.round(myVerScale) ){        return true;    }    else{        return false;    }}