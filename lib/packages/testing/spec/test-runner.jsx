//@include "../lib/jasmine-2.0.3/jasmine.js"//@include "extendscript-library-test.js"var TestRunner = function () {    this.clean_results = function (suites, results) {    }    this.run = function () {        var reporter = new jasmine.JsApiReporter();        jasmine.getEnv().addReporter(reporter);        var result = jasmine.getEnv().execute();        $.writeln(result);    }    this.get_environment = function () {        return {            'OS': $.os,            'ExtendScript build': $.build,            'ExtendScript version': $.version,            'path': $.includePath,            'locale': $.locale        }    }    this.to_console = function () {        var results = this.run();    }    this.to_html = function (filename) { }}var test_runner = new TestRunner();test_runner.run();