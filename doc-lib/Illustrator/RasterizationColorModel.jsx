/*
 * Controls the color model for the rasterization.
 */
var RasterizationColorModel = {
  /*
   * Rasterize in the default document color space.
   * @type {int}
   */
  DEFAULTCOLORMODEL: undefined,
  
  /*
   * Rasterize as grayscale image.
   * @type {int}
   */
  GRAYSCALE: undefined,
  
  /*
   * Rasterize as 1-bit bitmap.
   * @type {int}
   */
  BITMAP: undefined,
  
};
