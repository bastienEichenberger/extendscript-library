/*
 * PDF destination profile.
 */
var ColorDestination = {
  /*
   * N/A.
   * @type {int}
   */
  None: undefined,
  
  /*
   * Converts all colors to the profile selected for Destination.
   * @type {int}
   */
  COLORDESTINATIONDOCCMYK: undefined,
  
  /*
   * Preserves color numbers for untagged content in the same color space as the destination profile. Not available with CMS off.
   * @type {int}
   */
  COLORDESTINATIONWORKINGCMYK: undefined,
  
  /*
   * Preserves color numbers for untagged content in the same color space as the destination profile. Not available with CMS off.
   * @type {int}
   */
  COLORDESTINATIONDOCRGB: undefined,
  
  /*
   * Preserves color numbers for untagged content in the same color space as the destination profile. Not available with CMS off.
   * @type {int}
   */
  COLORDESTINATIONWORKINGRGB: undefined,
  
  /*
   * Preserves color numbers for untagged content in the same color space as the destination profile. Not available with CMS off.
   * @type {int}
   */
  COLORDESTINATIONPROFILE: undefined,
  
};
