/*
 * Style options for kenten characters.
 */
var KentenCharacter = {
  /*
   * Does not use kenten.
   * @type {Number}
   */
  NONE: undefined,
  
  /*
   * Uses the kenten sesame dot.
   * @type {Number}
   */
  KENTEN_SESAME_DOT: undefined,
  
  /*
   * Uses the kenten white sesame dot.
   * @type {Number}
   */
  KENTEN_WHITE_SESAME_DOT: undefined,
  
  /*
   * Uses the kenten black circle.
   * @type {Number}
   */
  KENTEN_BLACK_CIRCLE: undefined,
  
  /*
   * Uses the kenten white circle.
   * @type {Number}
   */
  KENTEN_WHITE_CIRCLE: undefined,
  
  /*
   * Uses the kenten black triangle.
   * @type {Number}
   */
  KENTEN_BLACK_TRIANGLE: undefined,
  
  /*
   * Uses the kenten white triangle.
   * @type {Number}
   */
  KENTEN_WHITE_TRIANGLE: undefined,
  
  /*
   * Uses the kenten bullseye.
   * @type {Number}
   */
  KENTEN_BULLSEYE: undefined,
  
  /*
   * Uses the kenten fisheye.
   * @type {Number}
   */
  KENTEN_FISHEYE: undefined,
  
  /*
   * Uses the kenten small black circle.
   * @type {Number}
   */
  KENTEN_SMALL_BLACK_CIRCLE: undefined,
  
  /*
   * Uses the kenten small white circle.
   * @type {Number}
   */
  KENTEN_SMALL_WHITE_CIRCLE: undefined,
  
  /*
   * Uses a custom kenten style.
   * @type {Number}
   */
  CUSTOM: undefined,
  
};
