/*
 * Conflict Resolution Choice.
 */
var ConflictResolutionChoiceEnum = {
  /*
   * Syncs the cloud version.
   * @type {Number}
   */
  SERVER_WINS: undefined,
  
  /*
   * Syncs the local machine version.
   * @type {Number}
   */
  LOCAL_WINS: undefined,
  
  /*
   * Asks the user always to resolve conflicts.
   * @type {Number}
   */
  ASK_ME: undefined,
  
};
