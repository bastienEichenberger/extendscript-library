/*
 * Print layer options
 */
var PrintLayerOptions = {
  /*
   * Prints all layers.
   * @type {Number}
   */
  ALL_LAYERS: undefined,
  
  /*
   * Prints all visible layers.
   * @type {Number}
   */
  VISIBLE_LAYERS: undefined,
  
  /*
   * Prints only layers that are both visible and printable.
   * @type {Number}
   */
  VISIBLE_PRINTABLE_LAYERS: undefined,
  
};
