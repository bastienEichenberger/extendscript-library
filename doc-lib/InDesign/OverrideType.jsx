/*
 * Override clearing options.
 */
var OverrideType = {
  /*
   * Clears all types of override.
   * @type {Number}
   */
  ALL: undefined,
  
  /*
   * Clears only character style overrides.
   * @type {Number}
   */
  CHARACTER_ONLY: undefined,
  
  /*
   * Clears only paragraph style overrides.
   * @type {Number}
   */
  PARAGRAPH_ONLY: undefined,
  
};
