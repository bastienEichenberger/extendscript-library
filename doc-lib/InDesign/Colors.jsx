/*
 * A collection of colors.
 */
var Colors = {
  /*
   * The number of objects in the collection.
   * @type {Number}
   */
  length: undefined,
  
  /*
   * Displays the number of elements in the Color.
   * @returns {Number}
   */
  count: function() {
    return new Number();
  },
  
  /*
   * Creates a new Color.
 *
   * @param {Object} [withProperties] Initial values for properties of the new Color 
   * @returns {Color}
   */
  add: function(withProperties) {
    return new Color();
  },
  
  /*
   * Returns the Color with the specified index or name.
 *
   * @param {Mixed} index The index or name. Can accept: Long Integer or String.
   * @returns {Color}
   */
  item: function(index) {
    return new Color();
  },
  
  /*
   * Returns the Color with the specified name.
 *
   * @param {String} name The name.
   * @returns {Color}
   */
  itemByName: function(name) {
    return new Color();
  },
  
  /*
   * Returns the Color with the specified ID.
 *
   * @param {Number} id The ID.
   * @returns {Color}
   */
  itemByID: function(id) {
    return new Color();
  },
  
  /*
   * Returns the Colors within the specified range.
 *
   * @param {Mixed} from The Color, index, or name at the beginning of the range. Can accept: Color, Long Integer or String.
   * @param {Mixed} to The Color, index, or name at the end of the range. Can accept: Color, Long Integer or String.
   * @returns {Color}
   */
  itemByRange: function(from, to) {
    return new Color();
  },
  
  /*
   * Returns the first Color in the collection.
   * @returns {Color}
   */
  firstItem: function() {
    return new Color();
  },
  
  /*
   * Returns the last Color in the collection.
   * @returns {Color}
   */
  lastItem: function() {
    return new Color();
  },
  
  /*
   * Returns the middle Color in the collection.
   * @returns {Color}
   */
  middleItem: function() {
    return new Color();
  },
  
  /*
   * Returns the Color with the index previous to the specified index.
 *
   * @param {Color} obj The index of the Color that follows the desired Color.
   * @returns {Color}
   */
  previousItem: function(obj) {
    return new Color();
  },
  
  /*
   * Returns the Color whose index follows the specified Color in the collection.
 *
   * @param {Color} obj The Color whose index comes before the desired Color.
   * @returns {Color}
   */
  nextItem: function(obj) {
    return new Color();
  },
  
  /*
   * Returns any Color in the collection.
   * @returns {Color}
   */
  anyItem: function() {
    return new Color();
  },
  
  /*
   * Returns every Color in the collection.
   * @returns {Color}
   */
  everyItem: function() {
    return new Color();
  },
  
  /*
   * Generates a string which, if executed, will return the Color.
   * @returns {String}
   */
  toSource: function() {
    return new String();
  },
  
  /*
   * Returns the Color with the specified index.
 *
   * @param {Number} index The index.
   * @returns {Color}
   */
  []: function(index) {
    return new Color();
  },
  
};
