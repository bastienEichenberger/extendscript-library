/*
 * Export format options.
 */
var ExportFormat = {
  /*
   * Exports to a tagged text file with a TXT extension.
   * @type {Number}
   */
  TAGGED_TEXT: undefined,
  
  /*
   * Exports to PDF format.
   * @type {Number}
   */
  PDF_TYPE: undefined,
  
  /*
   * Exports to EPS format.
   * @type {Number}
   */
  EPS_TYPE: undefined,
  
  /*
   * Exports to rich text format (RTF).
   * @type {Number}
   */
  RTF: undefined,
  
  /*
   * Exports to text (TXT) format.
   * @type {Number}
   */
  TEXT_TYPE: undefined,
  
  /*
   * Exports the document's tagged content to XML.
   * @type {Number}
   */
  XML: undefined,
  
  /*
   * Exports to JPEG format.
   * @type {Number}
   */
  JPG: undefined,
  
  /*
   * Exports to Interactive PDF format.
   * @type {Number}
   */
  INTERACTIVE_PDF: undefined,
  
  /*
   * Exports to packaged XFL format.
   * @type {Number}
   */
  PACKAGED_XFL: undefined,
  
  /*
   * Exports to SWF format.
   * @type {Number}
   */
  SWF: undefined,
  
  /*
   * Exports to InDesign snippet (IDMS) format.
   * @type {Number}
   */
  INDESIGN_SNIPPET: undefined,
  
  /*
   * Exports to InDesign markup (IDML) format.
   * @type {Number}
   */
  INDESIGN_MARKUP: undefined,
  
  /*
   * Exports to InCopy markup (ICML) format.
   * @type {Number}
   */
  INCOPY_MARKUP: undefined,
  
  /*
   * Exports to PNG format.
   * @type {Number}
   */
  PNG_FORMAT: undefined,
  
  /*
   * Exports to XHTML format.
   * @type {Number}
   */
  HTML: undefined,
  
  /*
   * Exports to EPub format.
   * @type {Number}
   */
  EPUB: undefined,
  
};
