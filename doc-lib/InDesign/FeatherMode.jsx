/*
 * On/off options for feathering.
 */
var FeatherMode = {
  /*
   * Does not use feathering.
   * @type {Number}
   */
  NONE: undefined,
  
  /*
   * Uses standard feathering.
   * @type {Number}
   */
  STANDARD: undefined,
  
};
