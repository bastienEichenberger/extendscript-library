/*
 * Color palette options for GIF conversion.
 */
var GIFOptionsPalette = {
  /*
   * Uses the adaptive (no dither) palette.
   * @type {Number}
   */
  ADAPTIVE_PALETTE: undefined,
  
  /*
   * Uses the Macintosh palette.
   * @type {Number}
   */
  MACINTOSH_PALETTE: undefined,
  
  /*
   * Uses the Web palette.
   * @type {Number}
   */
  WEB_PALETTE: undefined,
  
  /*
   * Uses the Windows palette.
   * @type {Number}
   */
  WINDOWS_PALETTE: undefined,
  
};
