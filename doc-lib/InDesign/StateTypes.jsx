/*
 * Options for specifying the events &amp;&#35;40;user actions&#41; that change a button&apos;s state.
 */
var StateTypes = {
  /*
   * The default appearance, used when there is no user activity on the button's area.
   * @type {Number}
   */
  UP: undefined,
  
  /*
   * The mouse pointer moves into the button's area.
   * @type {Number}
   */
  ROLLOVER: undefined,
  
  /*
   * The mouse pointer is clicked on the button's area.
   * @type {Number}
   */
  DOWN: undefined,
  
  /*
   * Up on state.
   * @type {Number}
   */
  UP_ON: undefined,
  
  /*
   * Rollover on state.
   * @type {Number}
   */
  ROLLOVER_ON: undefined,
  
  /*
   * Down on state.
   * @type {Number}
   */
  DOWN_ON: undefined,
  
  /*
   * Up off state.
   * @type {Number}
   */
  UP_OFF: undefined,
  
  /*
   * Rollover off state.
   * @type {Number}
   */
  ROLLOVER_OFF: undefined,
  
  /*
   * Down off state.
   * @type {Number}
   */
  DOWN_OFF: undefined,
  
};
