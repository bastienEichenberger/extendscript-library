/*
 * Options for the target of the active stroke/fill proxy.
 */
var StrokeFillTargetOptions = {
  /*
   * Formatting affects the container.
   * @type {Number}
   */
  FORMATTING_AFFECTS_CONTAINER: undefined,
  
  /*
   * Formatting affects the text.
   * @type {Number}
   */
  FORMATTING_AFFECTS_TEXT: undefined,
  
};
