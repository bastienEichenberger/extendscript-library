/*
 * Options for resolving style conflicts when importing tagged text.
 */
var StyleConflict = {
  /*
   * Uses the publication style.
   * @type {Number}
   */
  PUBLICATION_DEFINITION: undefined,
  
  /*
   * Uses the tag file style.
   * @type {Number}
   */
  TAG_FILE_DEFINITION: undefined,
  
};
