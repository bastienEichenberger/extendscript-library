/*
 * Glyph variant substitution options for standard glyphs.
 */
var AlternateGlyphForms = {
  /*
   * Does not use an alternate form.
   * @type {Number}
   */
  NONE: undefined,
  
  /*
   * Uses the traditional variant.
   * @type {Number}
   */
  TRADITIONAL_FORM: undefined,
  
  /*
   * Uses the expert variant.
   * @type {Number}
   */
  EXPERT_FORM: undefined,
  
  /*
   * Uses the JIS78 variant.
   * @type {Number}
   */
  JIS78_FORM: undefined,
  
  /*
   * Uses the JIS83 variant.
   * @type {Number}
   */
  JIS83_FORM: undefined,
  
  /*
   * Uses the monospaced half-width variant.
   * @type {Number}
   */
  MONOSPACED_HALF_WIDTH_FORM: undefined,
  
  /*
   * Uses the third-width variant.
   * @type {Number}
   */
  THIRD_WIDTH_FORM: undefined,
  
  /*
   * Uses the quarter-width variant.
   * @type {Number}
   */
  QUARTER_WIDTH_FORM: undefined,
  
  /*
   * Uses the NLC variant.
   * @type {Number}
   */
  NLC_FORM: undefined,
  
  /*
   * Substitutes proportional glyphs for half-width and full-width glyphs.
   * @type {Number}
   */
  PROPORTIONAL_WIDTH_FORM: undefined,
  
  /*
   * Uses the full-width variant.
   * @type {Number}
   */
  FULL_WIDTH_FORM: undefined,
  
  /*
   * Uses the JIS04 variant.
   * @type {Number}
   */
  JIS04_FORM: undefined,
  
  /*
   * Uses the JIS90 variant.
   * @type {Number}
   */
  JIS90_FORM: undefined,
  
};
