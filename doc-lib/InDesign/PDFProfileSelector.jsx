/*
 * The color profile.
 */
var PDFProfileSelector = {
  /*
   * Uses no profile.
   * @type {Number}
   */
  USE_NO_PROFILE: undefined,
  
  /*
   * Uses the monitor's color profile.
   * @type {Number}
   */
  USE_MONITOR_PROFILE: undefined,
  
  /*
   * Uses the document's CMYK profile.
   * @type {Number}
   */
  USE_DOCUMENT: undefined,
  
  /*
   * Uses the working CMYK profile.
   * @type {Number}
   */
  WORKING: undefined,
  
};
