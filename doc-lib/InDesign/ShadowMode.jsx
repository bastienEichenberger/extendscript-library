/*
 * Shadow mode options.
 */
var ShadowMode = {
  /*
   * Does not use a shadow.
   * @type {Number}
   */
  NONE: undefined,
  
  /*
   * Uses a standard blurred drop shadow.
   * @type {Number}
   */
  DROP: undefined,
  
};
