/*
 * Page scaling options.
 */
var ScaleModes = {
  /*
   * Scales the page width and height.
   * @type {Number}
   */
  SCALE_WIDTH_HEIGHT: undefined,
  
  /*
   * Scales the page to fit the paper. Note: Valid only when tile is false.
   * @type {Number}
   */
  SCALE_TO_FIT: undefined,
  
};
