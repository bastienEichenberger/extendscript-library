/*
 * A collection of fonts.
 */
var Fonts = {
  /*
   * The number of objects in the collection.
   * @type {Number}
   */
  length: undefined,
  
  /*
   * Displays the number of elements in the Font.
   * @returns {Number}
   */
  count: function() {
    return new Number();
  },
  
  /*
   * Returns the Font with the specified index or name.
 *
   * @param {Mixed} index The index or name. Can accept: Long Integer or String.
   * @returns {Font}
   */
  item: function(index) {
    return new Font();
  },
  
  /*
   * Returns the Font with the specified name.
 *
   * @param {String} name The name.
   * @returns {Font}
   */
  itemByName: function(name) {
    return new Font();
  },
  
  /*
   * Returns the Fonts within the specified range.
 *
   * @param {Mixed} from The Font, index, or name at the beginning of the range. Can accept: Font, Long Integer or String.
   * @param {Mixed} to The Font, index, or name at the end of the range. Can accept: Font, Long Integer or String.
   * @returns {Font}
   */
  itemByRange: function(from, to) {
    return new Font();
  },
  
  /*
   * Returns the first Font in the collection.
   * @returns {Font}
   */
  firstItem: function() {
    return new Font();
  },
  
  /*
   * Returns the last Font in the collection.
   * @returns {Font}
   */
  lastItem: function() {
    return new Font();
  },
  
  /*
   * Returns the middle Font in the collection.
   * @returns {Font}
   */
  middleItem: function() {
    return new Font();
  },
  
  /*
   * Returns the Font with the index previous to the specified index.
 *
   * @param {Font} obj The index of the Font that follows the desired Font.
   * @returns {Font}
   */
  previousItem: function(obj) {
    return new Font();
  },
  
  /*
   * Returns the Font whose index follows the specified Font in the collection.
 *
   * @param {Font} obj The Font whose index comes before the desired Font.
   * @returns {Font}
   */
  nextItem: function(obj) {
    return new Font();
  },
  
  /*
   * Returns any Font in the collection.
   * @returns {Font}
   */
  anyItem: function() {
    return new Font();
  },
  
  /*
   * Returns every Font in the collection.
   * @returns {Font}
   */
  everyItem: function() {
    return new Font();
  },
  
  /*
   * Generates a string which, if executed, will return the Font.
   * @returns {String}
   */
  toSource: function() {
    return new String();
  },
  
  /*
   * Returns the Font with the specified index.
 *
   * @param {Number} index The index.
   * @returns {Font}
   */
  []: function(index) {
    return new Font();
  },
  
};
