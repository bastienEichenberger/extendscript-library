/*
 * Ruby overhang options.
 */
var RubyOverhang = {
  /*
   * Does not allow ruby overhang.
   * @type {Number}
   */
  NONE: undefined,
  
  /*
   * Ruby overhang is one ruby.
   * @type {Number}
   */
  RUBY_OVERHANG_ONE_RUBY: undefined,
  
  /*
   * Ruby overhang is one-half ruby.
   * @type {Number}
   */
  RUBY_OVERHANG_HALF_RUBY: undefined,
  
  /*
   * Ruby overhang is the size of one character.
   * @type {Number}
   */
  RUBY_OVERHANG_ONE_CHAR: undefined,
  
  /*
   * Ruby is overhang one-half the size of one chararacter.
   * @type {Number}
   */
  RUBY_OVERHANG_HALF_CHAR: undefined,
  
  /*
   * There is no ruby overhang size limit.
   * @type {Number}
   */
  RUBY_OVERHANG_NO_LIMIT: undefined,
  
};
