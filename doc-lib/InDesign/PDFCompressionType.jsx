/*
 * The objects to compress in the PDF document.
 */
var PDFCompressionType = {
  /*
   * Uses no compression.
   * @type {Number}
   */
  COMPRESS_NONE: undefined,
  
  /*
   * Compresses only objects related to PDF structure.
   * @type {Number}
   */
  COMPRESS_STRUCTURE: undefined,
  
  /*
   * Compress all objects.
   * @type {Number}
   */
  COMPRESS_OBJECTS: undefined,
  
};
