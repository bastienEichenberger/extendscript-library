/*
 * Options for page color label.
 */
var PageColorOptions = {
  /*
   * No color.
   * @type {Number}
   */
  NOTHING: undefined,
  
  /*
   * Uses the color label of the page's master page.
   * @type {Number}
   */
  USE_MASTER_COLOR: undefined,
  
};
