/*
 * The device types that are attached.
 */
var AttachedDevices = {
  /*
   * all devices, Android followed by iOS.
   * @type {Number}
   */
  ALL: undefined,
  
  /*
   * Android devices.
   * @type {Number}
   */
  ANDROID: undefined,
  
  /*
   * iOS devices.
   * @type {Number}
   */
  IOS: undefined,
  
};
