/*
 * Options for specifying a feature set.
 */
var FeatureSetOptions = {
  /*
   * Uses the Roman feature set and defaults.
   * @type {Number}
   */
  ROMAN: undefined,
  
  /*
   * Uses the Japanese feature set and defaults.
   * @type {Number}
   */
  JAPANESE: undefined,
  
  /*
   * Uses the R2L feature set
   * @type {Number}
   */
  RIGHTTOLEFT: undefined,
  
};
