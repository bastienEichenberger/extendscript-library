/*
 * The tag type of page item
 */
var TagType = {
  /*
   * Determine the tag from XML structure, or fallback to standard tag
   * @type {Number}
   */
  TAG_FROM_STRUCTURE: undefined,
  
  /*
   * Tag as artifact
   * @type {Number}
   */
  TAG_ARTIFACT: undefined,
  
  /*
   * Tag as Story or Figure based on object type
   * @type {Number}
   */
  TAG_BASED_ON_OBJECT: undefined,
  
};
