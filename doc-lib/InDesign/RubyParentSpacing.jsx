/*
 * Options for ruby spacing relative to the parent text.
 */
var RubyParentSpacing = {
  /*
   * Does not base ruby spacing on parent text.
   * @type {Number}
   */
  RUBY_PARENT_NO_ADJUSTMENT: undefined,
  
  /*
   * Ruby parent both sides.
   * @type {Number}
   */
  RUBY_PARENT_BOTH_SIDES: undefined,
  
  /*
   * Ruby parent 121 aki.
   * @type {Number}
   */
  RUBY_PARENT_121_AKI: undefined,
  
  /*
   * Applies the parent text aki to the ruby characters.
   * @type {Number}
   */
  RUBY_PARENT_EQUAL_AKI: undefined,
  
  /*
   * Justifies ruby characters to both edges of the parent text.
   * @type {Number}
   */
  RUBY_PARENT_FULL_JUSTIFY: undefined,
  
};
