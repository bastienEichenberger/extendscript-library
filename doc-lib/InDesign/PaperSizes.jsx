/*
 * Paper size optons.
 */
var PaperSizes = {
  /*
   * Allows the printer driver to define the paper size.
   * @type {Number}
   */
  DEFINED_BY_DRIVER: undefined,
  
  /*
   * Allows definition of a custom paper size. Note: Not all printers allow custom paper sizes.
   * @type {Number}
   */
  CUSTOM: undefined,
  
};
