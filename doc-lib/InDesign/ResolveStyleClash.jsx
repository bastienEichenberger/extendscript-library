/*
 * Options for resolving clashes that result from matching style names.
 */
var ResolveStyleClash = {
  /*
   * Uses the existing style.
   * @type {Number}
   */
  RESOLVE_CLASH_USE_EXISTING: undefined,
  
  /*
   * Automatically renames the new style.
   * @type {Number}
   */
  RESOLVE_CLASH_AUTO_RENAME: undefined,
  
  /*
   * Uses the new style.
   * @type {Number}
   */
  RESOLVE_CLASH_USE_NEW: undefined,
  
};
