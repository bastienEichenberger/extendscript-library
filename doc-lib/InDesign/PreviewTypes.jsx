/*
 * Preview image file format options.
 */
var PreviewTypes = {
  /*
   * Does not save a preview image.
   * @type {Number}
   */
  NONE: undefined,
  
  /*
   * Saves the preview in TIFF format.
   * @type {Number}
   */
  TIFF_PREVIEW: undefined,
  
  /*
   * Saves the preview in PICT format. Note: Valid only for Mac OS.
   * @type {Number}
   */
  PICT_PREVIEW: undefined,
  
};
