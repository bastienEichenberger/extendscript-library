/*
 * Export layer options.
 */
var ExportLayerOptions = {
  /*
   * Export all layers
   * @type {Number}
   */
  EXPORT_ALL_LAYERS: undefined,
  
  /*
   * Export visible layers
   * @type {Number}
   */
  EXPORT_VISIBLE_LAYERS: undefined,
  
  /*
   * Export visible and printable layers
   * @type {Number}
   */
  EXPORT_VISIBLE_PRINTABLE_LAYERS: undefined,
  
};
