/*
 * PDF raster compression options.
 */
var PDFRasterCompressionOptions = {
  /*
   * Uses JPEG compression.
   * @type {Number}
   */
  JPEG_COMPRESSION: undefined,
  
  /*
   * Uses the best quality type.
   * @type {Number}
   */
  LOSSLESS_COMPRESSION: undefined,
  
  /*
   * Uses JPEG compression and automatically determines the best quality type.
   * @type {Number}
   */
  AUTOMATIC_COMPRESSION: undefined,
  
};
