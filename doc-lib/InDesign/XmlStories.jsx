/*
 * A collection of xml stories.
 */
var XmlStories = {
  /*
   * The number of objects in the collection.
   * @type {Number}
   */
  length: undefined,
  
  /*
   * Displays the number of elements in the XmlStory.
   * @returns {Number}
   */
  count: function() {
    return new Number();
  },
  
  /*
   * Returns the XmlStory with the specified index or name.
 *
   * @param {Mixed} index The index or name. Can accept: Long Integer or String.
   * @returns {XmlStory}
   */
  item: function(index) {
    return new XmlStory();
  },
  
  /*
   * Returns the XmlStory with the specified name.
 *
   * @param {String} name The name.
   * @returns {XmlStory}
   */
  itemByName: function(name) {
    return new XmlStory();
  },
  
  /*
   * Returns the XmlStory with the specified ID.
 *
   * @param {Number} id The ID.
   * @returns {XmlStory}
   */
  itemByID: function(id) {
    return new XmlStory();
  },
  
  /*
   * Returns the XmlStories within the specified range.
 *
   * @param {Mixed} from The XmlStory, index, or name at the beginning of the range. Can accept: XmlStory, Long Integer or String.
   * @param {Mixed} to The XmlStory, index, or name at the end of the range. Can accept: XmlStory, Long Integer or String.
   * @returns {XmlStory}
   */
  itemByRange: function(from, to) {
    return new XmlStory();
  },
  
  /*
   * Returns the first XmlStory in the collection.
   * @returns {XmlStory}
   */
  firstItem: function() {
    return new XmlStory();
  },
  
  /*
   * Returns the last XmlStory in the collection.
   * @returns {XmlStory}
   */
  lastItem: function() {
    return new XmlStory();
  },
  
  /*
   * Returns the middle XmlStory in the collection.
   * @returns {XmlStory}
   */
  middleItem: function() {
    return new XmlStory();
  },
  
  /*
   * Returns the XmlStory with the index previous to the specified index.
 *
   * @param {XmlStory} obj The index of the XmlStory that follows the desired XmlStory.
   * @returns {XmlStory}
   */
  previousItem: function(obj) {
    return new XmlStory();
  },
  
  /*
   * Returns the XmlStory whose index follows the specified XmlStory in the collection.
 *
   * @param {XmlStory} obj The XmlStory whose index comes before the desired XmlStory.
   * @returns {XmlStory}
   */
  nextItem: function(obj) {
    return new XmlStory();
  },
  
  /*
   * Returns any XmlStory in the collection.
   * @returns {XmlStory}
   */
  anyItem: function() {
    return new XmlStory();
  },
  
  /*
   * Returns every XmlStory in the collection.
   * @returns {XmlStory}
   */
  everyItem: function() {
    return new XmlStory();
  },
  
  /*
   * Generates a string which, if executed, will return the XmlStory.
   * @returns {String}
   */
  toSource: function() {
    return new String();
  },
  
  /*
   * Returns the XmlStory with the specified index.
 *
   * @param {Number} index The index.
   * @returns {XmlStory}
   */
  []: function(index) {
    return new XmlStory();
  },
  
};
