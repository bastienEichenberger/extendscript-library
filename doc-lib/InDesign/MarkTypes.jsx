/*
 * Options for printer marks formats.
 */
var MarkTypes = {
  /*
   * Uses the default format.
   * @type {Number}
   */
  DEFAULT_VALUE: undefined,
  
  /*
   * Uses J marks with a circle.
   * @type {Number}
   */
  J_MARK_WITH_CIRCLE: undefined,
  
  /*
   * Uses J marks without a circle.
   * @type {Number}
   */
  J_MARK_WITHOUT_CIRCLE: undefined,
  
};
