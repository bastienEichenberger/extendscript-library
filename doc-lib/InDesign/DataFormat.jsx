/*
 * Format options for image data.
 */
var DataFormat = {
  /*
   * Uses ASCII format.
   * @type {Number}
   */
  ASCII: undefined,
  
  /*
   * Uses binary format.
   * @type {Number}
   */
  BINARY: undefined,
  
};
