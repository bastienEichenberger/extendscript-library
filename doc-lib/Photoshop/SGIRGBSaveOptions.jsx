/*
 * Options for saving a document in SGI RGB format.
 */
var SGIRGBSaveOptions = {
  /*
   * If true, the alpha channels are saved.
   * @type {Boolean}
   */
  alphaChannels: undefined,
  
  /*
   * If true, the spot colors are saved.
   * @type {Boolean}
   */
  spotColors: undefined,
  
};
