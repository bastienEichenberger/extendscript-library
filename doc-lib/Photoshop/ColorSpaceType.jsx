/*
 * The type of color space.
 */
var ColorSpaceType = {
  /*
   * Use the Adobe RGB color space.
   * @type {int}
   */
  ADOBERGB: undefined,
  
  /*
   * Use the ColorMatch RGB color space.
   * @type {int}
   */
  COLORMATCHRGB: undefined,
  
  /*
   * Use the ProPhoto RGB color space.
   * @type {int}
   */
  PROPHOTORGB: undefined,
  
  /*
   * Use the sRGB color space.
   * @type {int}
   */
  SRGB: undefined,
  
};
