/*
 * Depth map source options.
 */
var DepthMapSource = {
  /*
   * No depth map in use.
   * @type {int}
   */
  NONE: undefined,
  
  /*
   * Uses the transparency channel for the depth map.
   * @type {int}
   */
  TRANSPARENCYCHANNEL: undefined,
  
  /*
   * Uses the layer mask for the depth map.
   * @type {int}
   */
  LAYERMASK: undefined,
  
  /*
   * Uses the image highlight for the depth map.
   * @type {int}
   */
  IMAGEHIGHLIGHT: undefined,
  
};
