/*
 * The cache to be targeted in a purge operation.
 */
var PurgeTarget = {
  /*
   * Clears the undo cache.
   * @type {int}
   */
  UNDOCACHES: undefined,
  
  /*
   * Deletes all history states from the History palette.
   * @type {int}
   */
  HISTORYCACHES: undefined,
  
  /*
   * Clears the clipboard.
   * @type {int}
   */
  CLIPBOARDCACHE: undefined,
  
  /*
   * Clears all caches.
   * @type {int}
   */
  ALLCACHES: undefined,
  
};
