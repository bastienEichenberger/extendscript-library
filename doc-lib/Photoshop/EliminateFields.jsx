/*
 * The type of fields to eliminate.
 */
var EliminateFields = {
  /*
   * Eliminate odd interlaced lines in a video image.
   * @type {int}
   */
  ODDFIELDS: undefined,
  
  /*
   * Eliminate even interlaced lines in a video image.
   * @type {int}
   */
  EVENFIELDS: undefined,
  
};
