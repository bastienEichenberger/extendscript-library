/*
 * A color definition used in the document.
 */
var SolidColor = {
  /*
   * The color model.
   * @type {ColorModel}
   */
  model: undefined,
  
  /*
   * The RGB color model.
   * @type {RGBColor}
   */
  rgb: undefined,
  
  /*
   * The grayscale color model.
   * @type {GrayColor}
   */
  gray: undefined,
  
  /*
   * The CMYK color model.
   * @type {CMYKColor}
   */
  cmyk: undefined,
  
  /*
   * The lab color model.
   * @type {LabColor}
   */
  lab: undefined,
  
  /*
   * The HSB color model.
   * @type {HSBColor}
   */
  hsb: undefined,
  
  /*
   * The nearest web color to the current color.
   * @type {RGBColor}
   */
  nearestWebColor: undefined,
  
  /*
   * The color model.
   * @type {ColorModel}
   */
  model: undefined,
  
  /*
   * The RGB color model.
   * @type {RGBColor}
   */
  rgb: undefined,
  
  /*
   * The grayscale color model.
   * @type {GrayColor}
   */
  gray: undefined,
  
  /*
   * The CMYK color model.
   * @type {CMYKColor}
   */
  cmyk: undefined,
  
  /*
   * The lab color model.
   * @type {LabColor}
   */
  lab: undefined,
  
  /*
   * The HSB color model.
   * @type {HSBColor}
   */
  hsb: undefined,
  
  /*
   * The nearest web color to the current color.
   * @type {RGBColor}
   */
  nearestWebColor: undefined,
  
  /*
   * Compares two colors.
 *
   * @param {SolidColor} color Another color to compare with.
   * @returns {Boolean}
   */
  isEqual: function(color) {
    return new Boolean();
  },
  
};
