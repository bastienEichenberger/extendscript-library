/*
 * Deprecated for Adobe Photoshop CS3, the make photo gallery method has moved to Bridge.
 */
var GalleryOptions = {
  /*
   * The style to use for laying out the web page.
   * @type {String}
   */
  layoutStyle: undefined,
  
  /*
   * The email address to show on the web page.
   * @type {String}
   */
  emailAddress: undefined,
  
  /*
   * Short web page extension .htm or long web page extension .html.
   * @type {Boolean}
   */
  useShortExtension: undefined,
  
  /*
   * Web page should use UTF-8 encoding.
   * @type {Boolean}
   */
  useUTF8Encoding: undefined,
  
  /*
   * Include all files found in sub folders of the input folder.
   * @type {Boolean}
   */
  includeSubFolders: undefined,
  
  /*
   * Add width and height attributes for images.
   * @type {Boolean}
   */
  addSizeAttributes: undefined,
  
  /*
   * Save all of the metadata in the JPEG files.
   * @type {Boolean}
   */
  preserveAllMetadata: undefined,
  
  /*
   * Options related to banner settings.
   * @type {GalleryBannerOptions}
   */
  bannerOptions: undefined,
  
  /*
   * Options related to images settings.
   * @type {GalleryImagesOptions}
   */
  imagesOptions: undefined,
  
  /*
   * Options related to thumbnail settings.
   * @type {GalleryThumbnailOptions}
   */
  thumbnailOptions: undefined,
  
  /*
   * Options related to custom color settings.
   * @type {GalleryCustomColorOptions}
   */
  customColorOptions: undefined,
  
  /*
   * Options related to security settings.
   * @type {GallerySecurityOptions}
   */
  securityOptions: undefined,
  
};
