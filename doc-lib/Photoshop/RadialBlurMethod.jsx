/*
 * The blur method to use.
 */
var RadialBlurMethod = {
  /*
   * Blurs along concentric circular lines at the specified degree of rotation.
   * @type {int}
   */
  SPIN: undefined,
  
  /*
   * Blurs along radial lines, as if zooming into or out of the image.
   * @type {int}
   */
  ZOOM: undefined,
  
};
