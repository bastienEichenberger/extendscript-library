/*
 * The location of history log data.
 */
var SaveLogItemsType = {
  /*
   * Save history log in file metadata.
   * @type {int}
   */
  METADATA: undefined,
  
  /*
   * Save history log in a text file.
   * @type {int}
   */
  LOGFILE: undefined,
  
  /*
   * Save history log in file metadata and a text file.
   * @type {int}
   */
  LOGFILEANDMETADATA: undefined,
  
};
